<link rel="stylesheet" href={{ asset('css/profile.css') }}>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3" crossorigin="anonymous"></script>
<nav class="navbar navbar-expand-lg fixed-top bg-white">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">
            <img src="{{ asset('img/logo_urca.JPG') }}" alt="logo urca servant de redirection vers l'accueil" class="logo-urca">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria -expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_redirecteur') }}">Mon profil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_contact') }}">Contact</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
<br><br><br><br>
<script>
    const a=document.querySelector('ul');
{% if user.roles[0]=='ROLE_ENTREPRISE' %}
        a.innerHTML+='<li class="nav-item"><a class="nav-link" href="{{ path('app_redirecteur') }}">Mes Stages</a></li>';
{% endif %}
{% if user.roles[0]=='ROLE_ETUDIANT' %}
    a.innerHTML+='<li class="nav-item"><a class="nav-link" href="{{ path('app_redirecteur') }}">Stages</a></li>' +
    '<li class="nav-item"><a class="nav-link" href="{{ path('app_sujet_ter') }}">Sujets TER</a></li>'+
        '<li class="nav-item"><a class="nav-link" href="{{ path('app_emploi_du_temps') }}">Emploi du temps</a></li>';
    {% endif %}
    {% if user.roles[0]=='ROLE_ENSEIGNANT' or user.roles[0]=='ROLE_ADMIN,ROLE_ENSEIGNANT'%}
    a.innerHTML+='<li class="nav-item"><a class="nav-link" href="{{ path('app_redirecteur') }}">Stages</a></li>' +
        '<li class="nav-item"><a class="nav-link" href="{{ path('app_sujet_ter') }}">Sujets TER</a></li>'+
        '<li class="nav-item"><a class="nav-link" href="{{ path('app_emploi_du_temps') }}">Emploi du temps</a></li>';
    {% if user.roles[0]=='ROLE_ADMIN,ROLE_ENSEIGNANT' %}
    a.innerHTML+='<li class="nav-item"><a class="nav-link" href="{{ path('admin') }}">Administration</a></li>'
    {% endif %}
    {% endif %}
</script>